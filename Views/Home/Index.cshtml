@{
    ViewData["Title"] = "Escape Room Springfield";
}
<!DOCTYPE html>
<html lang="es">
<head>
  <meta charset="UTF-8">
  <title>Escape Room Springfield</title>
  <link href="https://fonts.googleapis.com/css2?family=Fredoka:wght@400;600&display=swap" rel="stylesheet">
  <style>
    body {
      margin: 0;
      font-family: 'Fredoka', sans-serif;
      background: url('/img/personajestodos.jpeg') no-repeat center center fixed;
      background-size: cover;
      color: white;
    }

    .overlay {
      position: fixed;
      top: 0; left: 0;
      width: 100%; height: 100%;
      background-color: rgba(0, 0, 0, 0.4);
      z-index: 0;
    }

    .contenido {
      position: relative;
      z-index: 1;
      max-width: 900px;
      margin: 60px auto;
      background-color: rgba(255, 255, 255, 0.85);
      padding: 40px;
      border-radius: 20px;
      text-align: center;
      box-shadow: 0 8px 30px rgba(0,0,0,0.3);
      color: #333;
    }

    h1 {
      font-size: 3em;
      color: #f28b00;
      margin-bottom: 20px;
    }

    .btn-start, .btn-continue {
      background-color: #ff5722;
      color: white;
      font-size: 1.3em;
      padding: 15px 30px;
      border: none;
      border-radius: 12px;
      cursor: pointer;
      transition: background-color 0.3s, transform 0.2s;
    }

    .btn-start:hover, .btn-continue:hover {
      background-color: #e64a19;
      transform: scale(1.05);
    }

    .texto-historia {
      font-size: 1.4em;
      margin-bottom: 20px;
    }

    .texto-historia img {
      max-width: 300px;
      display: block;
      margin: 20px auto;
      border-radius: 12px;
      box-shadow: 0 4px 10px rgba(0,0,0,0.2);
    }

    input[type="text"] {
      padding: 10px;
      font-size: 1em;
      border-radius: 10px;
      border: 1px solid #ccc;
      width: 80%;
      max-width: 300px;
    }
  </style>
</head>
<body>
  <div class="overlay"></div>

  <div id="pantalla-inicio" class="contenido">
    <h1>Escape Room Springfield</h1>
    <p>¡Bienvenido al juego!, Lee todas las reglas con mucha atención y acuérdate bien las contraseñas</p>
    <button class="btn-start" onclick="mostrarTexto()">Comenzar</button>
  </div>

  <div id="pantalla-texto" class="contenido" style="display:none;">
    <div class="texto-historia">
      <p>Bart toca la puerta de su casa pero no le responde nadie</p>
      <img src="/img/Bart intro1.png" alt="Bart frente a la casa">
      <p><strong>Bart:</strong> Necesito encontrar a mi familia, ya se hizo tarde.</p>
      <img src="/img/Bart intro2.png" alt="Bart preocupado">
      <p><strong>Bart:</strong> Tengo que buscar a mi familia.</p>
      <img src="/img/Bart intro3.png" alt="Mapa de Springfield">
    </div>

    <p><strong>Ingresá tu nombre para continuar:</strong></p>
    <input type="text" id="nombreJugador" placeholder="Tu nombre">
    <br><br>
    <button class="btn-continue" id="btnContinuar" onclick="continuarJuego()" disabled>Continuar</button>
  </div>

  <script>
    function mostrarTexto() {
      document.getElementById('pantalla-inicio').style.display = 'none';
      document.getElementById('pantalla-texto').style.display = 'block';
    }

    const inputNombre = document.getElementById('nombreJugador');
    const btnContinuar = document.getElementById('btnContinuar');

    inputNombre.addEventListener('input', () => {
      btnContinuar.disabled = inputNombre.value.trim() === "";
    });

    function continuarJuego() {
      const nombre = inputNombre.value.trim();
      if (nombre !== "") {
        sessionStorage.setItem("nombreJugador", nombre);
        window.location.href = '/Home/Menu';
      }
    }
  </script>
</body>
</html>

